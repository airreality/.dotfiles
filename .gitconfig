[credential]
	helper = store
[fetch]
    prune = true
[rerere]
    enabled = true
[color]
    ui = auto
[user]
    name = airreality
    email = tonygpx@yandex.ru
[includeIf "gitdir:~/work/"]
    path = ~/work/.gitconfig
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
